```bash
git config --global user.name "Conrad Wang"
git config --global user.email "phys.cz.wang@outlook.com"
git config --list


git add [file1] [file2] ...
git add .
git add [dir]

git init
git log
git status
git reflog
git commit -m "描述"
```

```bash
git config --global user.name "user's name"
git config --global user.email "user's email"
git config --list


git add [file1] [file2] ...
git add .
git add [dir]

git init
git log
git status
git reflog
git commit -m "描述"
```
